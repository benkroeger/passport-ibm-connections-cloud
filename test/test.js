/*global describe, it */
'use strict';
var assert = require('assert');
var passportIbmConnectionsCloud = require('../');

describe('passport-ibm-connections-cloud node module', function () {
  it('must have at least one test', function () {
    passportIbmConnectionsCloud();
    assert(false, 'I was too lazy to write any tests. Shame on me.');
  });
});
